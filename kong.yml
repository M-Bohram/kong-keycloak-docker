_format_version: "1.1"

services:
  # - name: { service-name }
  #   url: { service URL }
  #   routes:
  #     - name: { route-name }
  #       paths:
  #         - { path }

  # Example
  # - name: hello-service
  #   url: http://localhost:8082
  #   routes:
  #     - name: hello-route
  #       paths:
  #         - /hello
# plugins:
# - name: rate-limiting
#   service: hello-service
#   config:
#     minute: 5
#     policy: local

